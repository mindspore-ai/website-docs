mindspore.ops.Logit
===================

.. py:class:: mindspore.ops.Logit(eps=-1.0)

    .. code-block::

        prim = ops.Logit(eps)
        out = prim(input)

    等价于

    .. code-block::

        ops.logit(input, eps)

    更多详情请查看： :func:`mindspore.ops.logit` 。
