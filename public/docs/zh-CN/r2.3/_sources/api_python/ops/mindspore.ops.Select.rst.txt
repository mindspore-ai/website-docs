mindspore.ops.Select
====================

.. py:class:: mindspore.ops.Select()

    .. code-block::

        prim = ops.Select()
        out = prim(cond, x, y)

    等价于

    .. code-block::

        ops.select(cond, x, y)

    更多详情请查看： :func:`mindspore.ops.select` 。
